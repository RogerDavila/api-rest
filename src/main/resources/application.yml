spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bap?useSSL=false
    username: root
    password: S4nt4T3ch2017
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
      naming:
        strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.MySQL5Dialect